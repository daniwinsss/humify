services:
  humify:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - humify-data:/app/data
    restart: unless-stopped

volumes:
  humify-data:
